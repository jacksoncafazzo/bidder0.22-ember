import { isMe } from 'bidder022/helpers/is-me';
import { module, test } from 'qunit';

module('Unit | Helper | is me');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isMe([42]);
  assert.ok(result);
});
